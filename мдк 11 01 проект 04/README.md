# Портфолио с системой авторизации

Веб-приложение с системой регистрации и авторизации по номеру телефона, а также личным портфолио.

> ⚠️ **Внимание:** Этот проект содержит намеренные уязвимости и ошибки для обучения код-ревью. Не используйте этот код в продакшене!

## 📋 Задание по код-ревью

Для студентов: см. файл [CODE_REVIEW_TASK.md](CODE_REVIEW_TASK.md) с подробным заданием по проведению код-ревью.

Для преподавателей: см. файл [SOLUTION_HINTS.md](SOLUTION_HINTS.md) со списком всех намеренно внесенных ошибок.

## Возможности

- ✅ Регистрация по номеру телефона и паролю
- ✅ Вход по номеру телефона и паролю
- ✅ Восстановление пароля с защитой от брутфорса
- ✅ Валидация данных (телефон, пароль)
- ✅ Защита от CSRF атак
- ✅ Личное портфолио после авторизации
- ✅ Безопасный выход из системы

## Требования

- Node.js (версия 14 или выше)
- PHP (версия 7.4 или выше)
- SQLite3

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd code-rew
```

2. Установите зависимости:
```bash
npm install
```

3. Инициализируйте базу данных:
```bash
php init_db.php
```

## Запуск

### Вариант 1: Через npm
```bash
npm start
```

### Вариант 2: Через скрипты
**Windows:**
```bash
start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

Приложение будет доступно по адресу: `http://localhost:3000`

**Примечание:** Убедитесь, что PHP установлен и доступен в PATH. Для проверки выполните:
```bash
php -v
```

### Тесты безопасности
```bash
npm test
```
Скрипт запускает `tests/security.test.php`, используя отдельную SQLite-базу через переменную `APP_DB_PATH`, чтобы не затронуть рабочие данные.

## Использование

### Регистрация
1. Перейдите на страницу регистрации
2. Введите номер телефона в формате `+7-ххх-ххх-хх-хх` (10 цифр)
3. Введите пароль (только английские буквы, цифры и символы)
4. Подтвердите пароль
5. Нажмите "Зарегистрироваться"

### Вход
1. Введите номер телефона и пароль
2. Нажмите "Войти"
3. После успешного входа вы будете перенаправлены на главную страницу портфолио

### Восстановление пароля
1. Перейдите на страницу восстановления пароля
2. Введите номер телефона
3. Введите код восстановления (для демонстрации код показывается на экране)
4. Установите новый пароль

## Безопасность

- **CSRF защита**: Все формы защищены токенами CSRF
- **Хеширование паролей**: Пароли хранятся в виде bcrypt хешей
- **Защита от брутфорса**: Ограничение попыток восстановления пароля (3 попытки, блокировка на 15 минут)
- **Валидация данных**: Строгая валидация телефона и пароля на стороне сервера и клиента
- **SQL инъекции**: Использование подготовленных запросов (prepared statements)
- **Сессии**: Id сессии регенерируется после входа, cookie имеют `HttpOnly`, `Secure`, `SameSite=Strict`, дополнительно проверяется fingerprint и токен в таблице `sessions`.
- **Восстановление доступа**: Коды генерируются через `random_int`, не отображаются пользователю и очищаются по истечении срока.

## Формат данных

### Телефон
- Формат: `+7-ххх-ххх-хх-хх`
- Только цифры (10 цифр после +7)
- Автоматическое форматирование при вводе

### Пароль
- Минимум 6 символов
- Только английские буквы, цифры и символы
- Русские символы не допускаются

## Структура проекта

```
code-rew/
├── server.js          # Node.js сервер
├── package.json       # Зависимости проекта
├── config.php         # Конфигурация и вспомогательные функции
├── init_db.php        # Инициализация базы данных
├── login.php          # Страница входа
├── register.php       # Страница регистрации
├── recovery.php       # Страница восстановления пароля
├── index.php          # Главная страница (портфолио)
├── database.db        # SQLite база данных (создается автоматически)
├── .gitignore         # Игнорируемые файлы
└── README.md          # Документация
```

## База данных

База данных SQLite содержит следующие таблицы:

- **users**: Пользователи (id, phone, password, created_at)
- **password_recovery**: Коды восстановления пароля (id, phone, code, attempts, created_at, expires_at)
- **sessions**: Сессии пользователей (id, user_id, session_token, created_at, expires_at)

## Документы код-ревью

Отчёт, чек-лист и комментарий находятся в папке `мдк 11 01 проект 04/`:

- `code-review-report.md` — список найденных проблем и исправлений.
- `security-checklist.md` — чек-лист для будущих проектов.
- `COMMENT.txt` — комментарий «код ревью».

## Лицензия

MIT

## Автор

Создано для демонстрации системы авторизации и портфолио.

